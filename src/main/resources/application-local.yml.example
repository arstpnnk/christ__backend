# Copy this file to `application-local.yml` and fill in your real local secrets.
# Never commit `application-local.yml` (it is ignored by `.gitignore`).
spring:
  datasource:
    # Format: jdbc:postgresql://HOST:PORT/DB_NAME
    url: jdbc:postgresql://localhost:5432/backend_db
    # Example: postgres
    username: postgres
    # Your local Postgres password (string)
    password: YOUR_DB_PASSWORD

  security:
    oauth2:
      client:
        registration:
          google:
            # Looks like: 12345-abcdefg.apps.googleusercontent.com
            client-id: YOUR_GOOGLE_CLIENT_ID
            # Looks like: GOCSPX-xxxxxxxxxxxxxxxxxxxx
            client-secret: YOUR_GOOGLE_CLIENT_SECRET

app:
  jwt:
    # Must be base64-encoded 32+ bytes (JWT signing key).
    secret: YOUR_BASE64_JWT_SECRET
